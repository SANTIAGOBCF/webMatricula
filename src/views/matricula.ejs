<% include partials/_header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-12 ">
      <% if(data){ %>
        <h1>Alumno : <%= data.nom %></h1> 
      <% } %>
    </div>
    <div class="col-12 ">
            <button type="submit" class="btn btn-primary">
              INICIAR MATRICULA
            </button>
    </div>
  </div>
</div>

<% include partials/_footer %>
